<style>
  /* ── Custom Window UI ── */
  .custom-window-overlay {
    position: fixed;
    inset: 0;
    background: rgba(15, 23, 42, 0.7);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    animation: fadeIn 0.3s ease;
    padding: 20px;
  }

  .custom-window {
    background: rgba(15, 23, 42, 0.9);
    border: 1px solid rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-radius: 2rem;
    max-width: 480px;
    width: 100%;
    overflow: hidden;
    box-shadow: 0 40px 100px -20px rgba(0, 0, 0, 0.7);
    animation: slideUp 0.5s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .window-header {
    background: rgba(255, 255, 255, 0.03);
    padding: 1.5rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .window-title {
    color: #f8fafc;
    font-weight: 800;
    font-size: 1.25rem;
    margin: 0;
    letter-spacing: -0.01em;
  }

  .window-body {
    padding: 2rem 1.5rem;
    color: #cbd5e1;
    font-size: 1.05rem;
    line-height: 1.6;
  }

  .window-footer {
    padding: 1.25rem 1.5rem;
    background: rgba(255, 255, 255, 0.02);
    display: flex;
    justify-content: flex-end;
    gap: 0.75rem;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
  }

  .window-btn {
    padding: 0.6rem 1.4rem;
    border-radius: 0.75rem;
    font-weight: 700;
    transition: all 0.2s;
    border: none;
    cursor: pointer;
  }

  .btn-confirm {
    background: #6366f1;
    color: white;
    box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3);
  }

  .btn-confirm:hover {
    background: #4f46e5;
    transform: translateY(-1px);
  }

  .btn-cancel {
    background: rgba(255, 255, 255, 0.08);
    color: #94a3b8;
  }

  .btn-cancel:hover {
    background: rgba(255, 255, 255, 0.12);
    color: #f1f5f9;
  }

  /* Window Types */
  .window-type-success .window-title {
    color: #10b981;
  }

  .window-type-warning .window-title {
    color: #f59e0b;
  }

  .window-type-alert .window-title {
    color: #f43f5e;
  }

  /* Focus Lost Overlay */
  .focus-lost-overlay {
    position: fixed;
    inset: 0;
    background: rgba(15, 23, 42, 0.9);
    backdrop-filter: blur(20px);
    z-index: 10000;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: white;
    text-align: center;
    padding: 2rem;
  }

  .focus-lost-icon {
    font-size: 5rem;
    margin-bottom: 1.5rem;
    filter: drop-shadow(0 0 15px rgba(244, 63, 94, 0.5));
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }

    to {
      opacity: 1;
    }
  }

  @keyframes slideUp {
    from {
      transform: translateY(20px);
      opacity: 0;
    }

    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  .loader-spinner {
    width: 60px;
    height: 60px;
    border: 5px solid rgba(255, 255, 255, 0.1);
    border-top-color: #f59e0b;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin-bottom: 20px;
  }

  @keyframes spin {
    to {
      transform: rotate(360deg);
    }
  }

  /* ── Help Bot Styles ── */
  .help-bot-container {
    position: fixed;
    bottom: 25px;
    right: 25px;
    z-index: 10000;
    transition: transform 0.3s ease;
  }

  .help-bot-bubble {
    width: 65px;
    height: 65px;
    background: linear-gradient(135deg, #4f46e5, #6366f1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.8rem;
    cursor: pointer;
    box-shadow: 0 10px 25px rgba(79, 70, 229, 0.4);
    transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    position: relative;
  }

  .help-bot-bubble:hover {
    transform: scale(1.1) rotate(5deg);
    box-shadow: 0 15px 35px rgba(79, 70, 229, 0.6);
  }

  .help-bot-bubble .pulse {
    position: absolute;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background: #4f46e5;
    opacity: 0.5;
    animation: botPulse 2s infinite;
    z-index: -1;
  }

  @keyframes botPulse {
    0% {
      transform: scale(1);
      opacity: 0.5;
    }

    100% {
      transform: scale(1.6);
      opacity: 0;
    }
  }

  .help-bot-window {
    position: absolute;
    bottom: 85px;
    right: 0;
    width: 320px;
    background: rgba(30, 41, 59, 0.95);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 1.5rem;
    overflow: hidden;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.4);
    display: none;
    flex-direction: column;
    animation: botSlideUp 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  @keyframes botSlideUp {
    from {
      opacity: 0;
      transform: translateY(20px) scale(0.95);
    }

    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  .bot-header {
    background: linear-gradient(to right, rgba(255, 255, 255, 0.05), transparent);
    padding: 1.25rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .bot-header h5 {
    color: #f8fafc;
    font-weight: 800;
    font-size: 1rem;
    margin: 0;
  }

  .bot-body {
    padding: 1.25rem;
    max-height: 400px;
    overflow-y: auto;
  }

  .bot-msg {
    background: rgba(255, 255, 255, 0.05);
    color: #cbd5e1;
    padding: 0.85rem 1rem;
    border-radius: 1rem 1rem 1rem 0;
    font-size: 0.95rem;
    line-height: 1.5;
    margin-bottom: 1rem;
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  .bot-options {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .bot-opt-btn {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: #f8fafc;
    padding: 0.6rem 1rem;
    border-radius: 0.75rem;
    font-size: 0.9rem;
    text-align: left;
    transition: all 0.2s;
    cursor: pointer;
    font-weight: 600;
  }

  .bot-opt-btn:hover {
    background: #4f46e5;
    border-color: #6366f1;
    transform: translateX(5px);
  }

  @media (max-width: 576px) {
    .help-bot-window {
      width: calc(100vw - 40px);
      right: -10px;
    }

    .help-bot-container {
      right: 20px;
      bottom: 20px;
    }
  }

  /* ── Ultra-Premium Modern Site Footer ── */
  .site-footer {
    background: radial-gradient(circle at 50% 0%, #0d1527 0%, #030712 100%);
    color: #94a3b8;
    margin-top: auto;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    position: relative;
    overflow: hidden;
    padding-top: 56px;
    padding-bottom: 0;
  }

  /* Glowing Top Multi-Color Gradient Line */
  .site-footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #38bdf8 0%, #6366f1 35%, #a855f7 70%, #ec4899 100%);
    box-shadow: 0 0 16px rgba(99, 102, 241, 0.6);
  }

  /* Ambient Glow Blobs */
  .footer-ambient-glow-1 {
    position: absolute;
    top: -80px;
    left: 10%;
    width: 320px;
    height: 320px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(99, 102, 241, 0.12) 0%, transparent 70%);
    pointer-events: none;
  }

  .footer-ambient-glow-2 {
    position: absolute;
    bottom: 20px;
    right: 5%;
    width: 280px;
    height: 280px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(168, 85, 247, 0.1) 0%, transparent 70%);
    pointer-events: none;
  }

  /* Quick Access Pills Rail */
  .footer-quick-pills {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 10px;
    margin-bottom: 40px;
    padding-bottom: 28px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }

  .footer-pill {
    padding: 8px 18px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 999px;
    color: #cbd5e1;
    font-size: 13px;
    font-weight: 600;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .footer-pill:hover {
    background: rgba(99, 102, 241, 0.18);
    border-color: rgba(99, 102, 241, 0.5);
    color: #fff;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(99, 102, 241, 0.25);
  }

  .footer-brand-section {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .footer-brand-logo-wrap {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    text-decoration: none;
  }

  .footer-brand-logo-img {
    height: 48px;
    width: 48px;
    border-radius: 12px;
    object-fit: cover;
    border: 1px solid rgba(255, 255, 255, 0.15);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4);
  }

  .footer-brand-name {
    font-size: 1.25rem;
    font-weight: 800;
    letter-spacing: -0.5px;
    color: #ffffff;
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .footer-brand-tag {
    font-size: 0.68rem;
    font-weight: 700;
    padding: 2px 8px;
    border-radius: 6px;
    background: rgba(99, 102, 241, 0.2);
    color: #818cf8;
    border: 1px solid rgba(99, 102, 241, 0.3);
    text-transform: uppercase;
  }

  .footer-brand-desc {
    color: #94a3b8;
    font-size: 0.9rem;
    line-height: 1.6;
    max-width: 360px;
    margin: 0;
  }

  /* Live Student Pulse Indicator */
  .footer-live-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 0.8rem;
    font-weight: 600;
    color: #4ade80;
    background: rgba(34, 197, 94, 0.08);
    border: 1px solid rgba(34, 197, 94, 0.2);
    padding: 5px 12px;
    border-radius: 20px;
    width: fit-content;
  }

  .footer-live-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: #22c55e;
    box-shadow: 0 0 8px #22c55e;
    animation: livePulse 2s infinite;
  }

  @keyframes livePulse {
    0%, 100% {
      transform: scale(1);
      opacity: 1;
    }
    50% {
      transform: scale(1.3);
      opacity: 0.6;
    }
  }

  /* Column Titles */
  .footer-title {
    color: #f8fafc;
    font-weight: 700;
    font-size: 0.98rem;
    letter-spacing: 0.3px;
    margin-bottom: 1.25rem;
    position: relative;
    display: flex;
    align-items: center;
    gap: 8px;
    text-transform: uppercase;
  }

  .footer-title-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #6366f1;
    box-shadow: 0 0 8px rgba(99, 102, 241, 0.8);
  }

  .footer-links-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .footer-link-item a {
    color: #94a3b8;
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 3px 0;
    transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .footer-link-item a:hover {
    color: #ffffff;
    transform: translateX(5px);
  }

  .footer-link-item i {
    font-size: 0.72rem;
    color: #6366f1;
    opacity: 0.7;
    transition: all 0.2s ease;
  }

  .footer-link-item a:hover i {
    color: #38bdf8;
    opacity: 1;
    transform: translateX(2px);
  }

  /* Social Icons */
  .social-icons {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    margin-top: 4px;
  }

  .social-btn {
    width: 40px;
    height: 40px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.04);
    color: #cbd5e1;
    text-decoration: none;
    border: 1px solid rgba(255, 255, 255, 0.08);
    transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
    font-size: 1.05rem;
  }

  .social-btn:hover {
    background: rgba(99, 102, 241, 0.2);
    border-color: #6366f1;
    color: #fff;
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(99, 102, 241, 0.3);
  }

  .social-btn.social-yt:hover {
    background: rgba(239, 68, 68, 0.2);
    border-color: #ef4444;
    color: #fff;
    box-shadow: 0 8px 20px rgba(239, 68, 68, 0.3);
  }

  .social-btn.social-wa:hover {
    background: rgba(34, 197, 94, 0.2);
    border-color: #22c55e;
    color: #fff;
    box-shadow: 0 8px 20px rgba(34, 197, 94, 0.3);
  }

  .social-btn.social-pin:hover {
    background: rgba(244, 63, 94, 0.2);
    border-color: #f43f5e;
    color: #fff;
    box-shadow: 0 8px 20px rgba(244, 63, 94, 0.3);
  }

  .social-btn.social-ig:hover {
    background: linear-gradient(135deg, rgba(225, 48, 108, 0.25), rgba(131, 58, 180, 0.25));
    border-color: #e1306c;
    color: #fff;
    box-shadow: 0 8px 20px rgba(225, 48, 108, 0.35);
  }

  .whatsapp-badge-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    background: linear-gradient(135deg, #22c55e 0%, #15803d 100%);
    color: #ffffff !important;
    padding: 12px 22px;
    border-radius: 12px;
    font-weight: 700;
    font-size: 0.92rem;
    text-decoration: none;
    box-shadow: 0 8px 24px rgba(34, 197, 94, 0.3);
    transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
    width: 100%;
    max-width: 280px;
    border: 1px solid rgba(255, 255, 255, 0.15);
  }

  .whatsapp-badge-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 30px rgba(34, 197, 94, 0.45);
    color: #ffffff !important;
  }

  /* Support Card */
  .footer-support-card {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, rgba(255, 255, 255, 0.02) 100%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 18px;
    padding: 24px 20px;
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    position: relative;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    max-width: 400px;
  }

  .footer-support-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, #6366f1, #38bdf8, transparent);
  }

  .footer-support-title {
    color: #ffffff;
    font-weight: 800;
    font-size: 1rem;
    margin-bottom: 8px;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .footer-support-desc {
    font-size: 0.86rem;
    color: #94a3b8;
    line-height: 1.55;
    margin-bottom: 16px;
  }

  .footer-gmail-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    background: linear-gradient(135deg, rgba(234, 67, 53, 0.15), rgba(99, 102, 241, 0.18));
    border: 1px solid rgba(234, 67, 53, 0.4);
    color: #ffffff !important;
    padding: 12px 16px;
    border-radius: 12px;
    font-size: 0.92rem;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
    width: 100%;
    box-shadow: 0 4px 16px rgba(234, 67, 53, 0.15);
    white-space: nowrap;
    letter-spacing: 0.1px;
  }

  .footer-gmail-btn:hover {
    background: linear-gradient(135deg, rgba(234, 67, 53, 0.28), rgba(99, 102, 241, 0.32));
    border-color: #ea4335;
    color: #ffffff !important;
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(234, 67, 53, 0.3);
  }

  .footer-faq-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.09);
    color: #cbd5e1 !important;
    padding: 9px 14px;
    border-radius: 10px;
    font-size: 0.86rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s ease;
    width: 100%;
  }

  .footer-faq-btn:hover {
    background: rgba(255, 255, 255, 0.09);
    border-color: rgba(255, 255, 255, 0.2);
    color: #ffffff !important;
    transform: translateY(-1px);
  }

  /* Bottom Bar */
  .footer-bottom {
    background: rgba(0, 0, 0, 0.5);
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    padding: 22px 0;
    margin-top: 48px;
  }

  .footer-legal-links {
    display: flex;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
    justify-content: flex-end;
  }

  .footer-legal-links a {
    color: #64748b;
    font-size: 0.84rem;
    font-weight: 500;
    text-decoration: none;
    transition: color 0.2s;
  }

  .footer-legal-links a:hover {
    color: #a5b4fc;
  }

  /* Responsive Rules */
  @media (max-width: 767.98px) {
    .site-footer {
      padding-top: 40px;
      padding-bottom: 90px;
    }

    .footer-quick-pills {
      justify-content: center;
      margin-bottom: 28px;
      padding-bottom: 20px;
    }

    .footer-brand-section {
      text-align: center;
      align-items: center;
      margin-bottom: 20px;
    }

    .footer-brand-desc {
      text-align: center;
      margin: 0 auto;
    }

    .social-icons {
      justify-content: center;
    }

    .whatsapp-badge-btn {
      margin: 0 auto;
    }

    .footer-title {
      font-size: 0.92rem;
      margin-bottom: 0.85rem;
    }

    .footer-link-item a {
      font-size: 0.86rem;
      padding: 5px 0;
    }

    .footer-legal-links {
      justify-content: center;
      margin-top: 10px;
      gap: 12px;
    }

    .footer-bottom {
      text-align: left;
      padding: 18px 0;
      margin-top: 32px;
    }
  }

  /* Ensure footer sticky bottom */
  body {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }
  body > main, body > section, body > .main-content {
    flex: 1 0 auto;
  }
  body > footer.site-footer {
    flex-shrink: 0;
  }
</style>


<!-- Store Cart Bubble Styles -->
<style>
  .store-cart-container {
    display: none; /* shown via JS only on store pages */
    position: fixed;
    bottom: 100px;
    right: 26px;
    z-index: 9998;
  }

  .store-cart-bubble {
    width: 58px;
    height: 58px;
    background: linear-gradient(135deg, #fb641b 0%, #ea580c 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #ffffff;
    font-size: 1.4rem;
    cursor: pointer;
    box-shadow: 0 8px 28px rgba(251, 100, 27, 0.5);
    position: relative;
    transition: transform 0.25s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.25s ease;
    border: 2px solid rgba(255, 255, 255, 0.25);
  }

  .store-cart-bubble:hover {
    transform: scale(1.1) translateY(-3px);
    box-shadow: 0 14px 36px rgba(251, 100, 27, 0.7);
  }

  .store-cart-bubble:active {
    transform: scale(0.96);
  }

  /* Pulsing ring animation */
  .store-cart-bubble .pulse {
    position: absolute;
    inset: -4px;
    border-radius: 50%;
    border: 2px solid rgba(251, 100, 27, 0.6);
    animation: cartBubblePulse 2.4s ease-out infinite;
    pointer-events: none;
  }

  @keyframes cartBubblePulse {
    0% { transform: scale(1); opacity: 0.7; }
    70% { transform: scale(1.35); opacity: 0; }
    100% { transform: scale(1.35); opacity: 0; }
  }

  /* Item count badge */
  .cart-badge {
    position: absolute;
    top: -6px;
    right: -6px;
    background: #ffffff;
    color: #ea580c;
    font-size: 0.72rem;
    font-weight: 900;
    min-width: 22px;
    height: 22px;
    border-radius: 11px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 4px;
    border: 2px solid #fb641b;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.25);
    animation: badgePop 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  @keyframes badgePop {
    from { transform: scale(0.5); opacity: 0; }
    to   { transform: scale(1);   opacity: 1; }
  }

  /* On mobile, move slightly up to avoid bottom nav overlap */
  @media (max-width: 767.98px) {
    .store-cart-container {
      bottom: 80px;
      right: 16px;
    }

    .store-cart-bubble {
      width: 52px;
      height: 52px;
      font-size: 1.2rem;
    }
  }
</style>

<!-- Store Cart Bubble -->
<div class="store-cart-container" id="storeCartContainer">
  <div class="store-cart-bubble" onclick="window.location.href='/store/cart'" title="View Cart">
    <i class="fa-solid fa-cart-shopping"></i>
    <div class="pulse"></div>
    <span class="cart-badge" id="floatingCartCount" style="display: none;">0</span>
  </div>
</div>


<footer class="site-footer">
  <div class="footer-ambient-glow-1"></div>
  <div class="footer-ambient-glow-2"></div>
  <div class="container position-relative" style="z-index: 2;">
    <div class="row g-4 justify-content-between">
      <!-- Brand & Community Superhub Column -->
      <div class="col-lg-4 col-md-12">
        <div class="footer-brand-section">
          <a href="/" class="footer-brand-logo-wrap">
            <img src="/assets/images/logo.jpg" alt="2AM Study Logo" class="footer-brand-logo-img">
            <div>
              <div class="footer-brand-name">
                2AM Study
              </div>
              <div style="font-size: 0.85rem; font-weight: 600; color: #cbd5e1; letter-spacing: 0.3px;">Student Productivity Ecosystem</div>
            </div>
          </a>

          <p class="footer-brand-desc">
            From late-night study sessions to academic excellence — empowering students with smart focus tools, study gear, and an inclusive community.
          </p>

          <div class="footer-live-badge">
            <span class="footer-live-dot"></span>
            <span>10,000+ Students Studying Active</span>
          </div>

          <a href="https://chat.whatsapp.com/Fq87TyqNwL4GAmxRsKUkid?mode=gi_t" target="_blank" rel="noopener noreferrer" class="whatsapp-badge-btn">
            <i class="fa-brands fa-whatsapp fa-lg"></i>
            <span>Join Student WhatsApp</span>
          </a>

          <div class="social-icons">
            <a href="https://youtube.com/@2amstudy?si=scn2pH77qJ3FpHAL" target="_blank" rel="noopener noreferrer" class="social-btn social-yt" title="YouTube"><i class="fa-brands fa-youtube"></i></a>
            <a href="https://chat.whatsapp.com/Fq87TyqNwL4GAmxRsKUkid?mode=gi_t" target="_blank" rel="noopener noreferrer" class="social-btn social-wa" title="WhatsApp"><i class="fa-brands fa-whatsapp"></i></a>
            <a href="http://pinterest.com/2amstudy" target="_blank" rel="noopener noreferrer" class="social-btn social-pin" title="Pinterest"><i class="fa-brands fa-pinterest"></i></a>
            <a href="https://www.instagram.com/2amstudyofficial/" target="_blank" rel="noopener noreferrer" class="social-btn social-ig" title="Instagram"><i class="fa-brands fa-instagram"></i></a>
          </div>
        </div>
      </div>

      <!-- Quick Links / Company -->
      <div class="col-6 col-lg-2 col-md-3">
        <h5 class="footer-title">
          <span class="footer-title-dot"></span> Company
        </h5>
        <ul class="footer-links-list">
          <li class="footer-link-item"><a href="/"><i class="fa-solid fa-chevron-right"></i> Home</a></li>
          <li class="footer-link-item"><a href="/about"><i class="fa-solid fa-chevron-right"></i> About Us</a></li>
          <li class="footer-link-item"><a href="/store"><i class="fa-solid fa-chevron-right"></i> Student Store</a></li>
          <li class="footer-link-item"><a href="/blog"><i class="fa-solid fa-chevron-right"></i> Study Blog</a></li>
          <li class="footer-link-item"><a href="/faqs"><i class="fa-solid fa-chevron-right"></i> FAQs (Q&amp;A)</a></li>
          <li class="footer-link-item"><a href="/contact"><i class="fa-solid fa-chevron-right"></i> Contact Us</a></li>
          <li class="footer-link-item"><a href="/admin" style="opacity: 0.45; font-size: 0.78rem;"><i class="fa-solid fa-lock"></i> Admin</a></li>
        </ul>
      </div>

      <!-- Study Tools Column -->
      <div class="col-6 col-lg-2 col-md-3">
        <h5 class="footer-title">
          <span class="footer-title-dot"></span> Study Tools
        </h5>
        <ul class="footer-links-list">
          <li class="footer-link-item"><a href="/timer"><i class="fa-solid fa-clock"></i> Focus Timer</a></li>
          <li class="footer-link-item"><a href="/resources"><i class="fa-solid fa-book-open"></i> Exam Resources</a></li>
          <li class="footer-link-item"><a href="/tasks"><i class="fa-solid fa-calendar-check"></i> Study Planner</a></li>
          <li class="footer-link-item"><a href="/music"><i class="fa-solid fa-headphones"></i> LoFi Beats</a></li>
          <li class="footer-link-item"><a href="/student-safety"><i class="fa-solid fa-shield-halved"></i> Safety Hub</a></li>
          <li class="footer-link-item"><a href="/utilities/cgpa-calculator"><i class="fa-solid fa-calculator"></i> CGPA Calc</a></li>
        </ul>
      </div>

      <!-- Student Support Column -->
      <div class="col-12 col-lg-4 col-md-6">
        <h5 class="footer-title">
          <span class="footer-title-dot"></span> Support &amp; Help
        </h5>
        <div class="footer-support-card">
          <div class="footer-support-title">
            <i class="fa-solid fa-headset text-warning"></i> 24/7 Academic Support
          </div>
          <p class="footer-support-desc">
            Got study roadblocks or order questions? Our student support desk is ready to help you anytime.
          </p>
          <a href="mailto:2amstudyofficial@gmail.com" class="footer-gmail-btn mb-2">
            <i class="fa-solid fa-envelope" style="color: #ea4335; font-size: 1.1rem;"></i> 2amstudyofficial@gmail.com
          </a>
          <a href="/faqs" class="footer-faq-btn">
            <i class="fa-solid fa-circle-question text-info"></i> Browse FAQs &amp; Help
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom Legal & Copyright Bar -->
  <div class="footer-bottom">
    <div class="container">
      <div class="row align-items-center">
        <div class="col-12 text-start">
          <p class="mb-0 small text-muted">© 2026 <strong>2 AM Study</strong>. All rights reserved. • Student-First Always 🎓</p>
        </div>
      </div>
    </div>
  </div>
</footer>

<script>
  /* Mobile/tablet: toggle the PDF Tools nested submenu on click */
  document.addEventListener('DOMContentLoaded', function () {
    var trigger = document.querySelector('.pdf-submenu-toggle');
    var submenu = trigger && trigger.nextElementSibling;
    if (trigger && submenu) {
      trigger.addEventListener('click', function (e) {
        if (window.innerWidth < 992) {
          e.preventDefault();
          submenu.classList.toggle('show');
        }
      });
    }
  });
</script>

<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"
  integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.min.js"
  integrity="sha384-7qAoOXltbVP82dhxHAUje59V5r2YsVfBafyUDxEdApLPmcdhBPg1DKg1ERo0BZlK" crossorigin="anonymous"></script>

<script>
  const tooltipTriggerList = document.querySelectorAll('[data-bs-toggle="tooltip"]');
  const tooltipList = [...tooltipTriggerList].map(tooltipTriggerEl => {
    return new bootstrap.Tooltip(tooltipTriggerEl);
  });
</script>
<script>
  const forms = document.querySelectorAll('.sendMailForm');
  forms.forEach(form => {
    form.addEventListener('submit', async event => {
      event.preventDefault();
      const submitButton = form.querySelector('button[type="submit"]');
      if (submitButton) submitButton.disabled = true;

      const formData = new FormData(form);
      formData.append('formName', form.dataset.formName || 'Website Contact Form');

      try {
        const payload = Object.fromEntries(formData.entries());

        const response = await fetch('/send-email', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify(payload)
        });
        const result = await response.json();

        if (response.ok) {
          alert(result.message || '🎉 Thanks for registering! We’re excited to have you with us.');
          form.reset();
        } else {
          alert(result.error || 'Unable to send your message. Please try again later.');
        }
      } catch (error) {
        console.error('Send email error:', error);
        alert('Unable to send your message. Please check your network and try again.');
      } finally {
        if (submitButton) submitButton.disabled = false;
      }
    });
  });
</script>
<div id="distraction-confirm-overlay" class="distraction-confirm-overlay d-none">
  <div class="distraction-confirm-modal">
    <p>This may distract you. Stay focused or continue?</p>
    <div class="d-flex justify-content-end gap-2">
      <button id="stay-focused-btn" class="btn btn-secondary btn-sm">Stay Focused</button>
      <button id="continue-redirect-btn" class="btn btn-primary btn-sm">Continue</button>
    </div>
  </div>
</div>

<div id="custom-window-overlay" class="custom-window-overlay d-none">
  <div class="custom-window">
    <div class="window-header">
      <h3 class="window-title" id="window-title">Notification</h3>
      <button type="button" class="btn-close btn-close-white" onclick="closeWindow()" aria-label="Close"></button>
    </div>
    <div class="window-body">
      <p id="window-message"></p>
      <div id="window-input-container" class="mt-3 d-none">
        <input type="text" id="window-input" class="form-control bg-dark text-white border-secondary rounded-3 p-3">
      </div>
    </div>
    <div class="window-footer">
      <button type="button" id="window-cancel-btn" class="window-btn btn-cancel">Cancel</button>
      <button type="button" id="window-confirm-btn" class="window-btn btn-confirm">OK</button>
    </div>
  </div>
</div>

<div id="focus-lost-overlay" class="focus-lost-overlay d-none">
  <div class="focus-lost-icon">🚫</div>
  <h2 class="display-4 fw-bold mb-3">Focus Interrupted!</h2>
  <p class="lead mb-4 text-slate-400" style="max-width: 500px;">Leaving the study tab breaks your deep focus state.
    We're here to help you stay on track! Ready to get back to it?</p>
  <button class="btn btn-primary btn-lg rounded-pill px-5 fw-bold"
    onclick="document.getElementById('focus-lost-overlay').classList.add('d-none')">I'm Back & Focused</button>
</div>

<script>
  const loginProtectedMessage = 'Please register first. We’ll connect you soon after registration.';
  const isLoggedIn = () => localStorage.getItem('isLoggedIn') === 'true' || localStorage.getItem('studentRegistered') === 'true';
  const triggerRegisterFlow = () => {
    const heroRegisterSection = document.getElementById('hero-register-section');
    if (heroRegisterSection) {
      heroRegisterSection.scrollIntoView({ behavior: 'smooth', block: 'center' });
      return;
    }

    const inlineRegisterButton = document.getElementById('register-button');
    if (inlineRegisterButton) {
      inlineRegisterButton.classList.remove('d-none');
      inlineRegisterButton.scrollIntoView({ behavior: 'smooth', block: 'center' });
      return;
    }

    if (window.location.pathname !== '/') {
      window.location.href = '/?openRegister=1';
    }
  };
  const markUserRegistered = () => {
    localStorage.setItem('isLoggedIn', 'true');
    localStorage.setItem('studentRegistered', 'true');
  };

  /* ── Custom Window UI Logic ── */
  const windowOverlay = document.getElementById('custom-window-overlay');
  const windowTitle = document.getElementById('window-title');
  const windowMessage = document.getElementById('window-message');
  const windowInputContainer = document.getElementById('window-input-container');
  const windowInput = document.getElementById('window-input');
  const windowConfirmBtn = document.getElementById('window-confirm-btn');
  const windowCancelBtn = document.getElementById('window-cancel-btn');
  let windowResolve = null;

  function showWindow(options = {}) {
    const { title = 'Notification', message = '', type = 'alert', defaultValue = '', placeholder = '' } = options;
    return new Promise(resolve => {
      windowResolve = resolve;
      windowTitle.textContent = title;
      windowMessage.textContent = message;
      windowOverlay.className = `custom-window-overlay window-type-${type}`;

      if (type === 'prompt') {
        windowInputContainer.classList.remove('d-none');
        windowInput.value = defaultValue;
        windowInput.placeholder = placeholder;
        windowCancelBtn.classList.remove('d-none');
      } else if (type === 'confirm') {
        windowInputContainer.classList.add('d-none');
        windowCancelBtn.classList.remove('d-none');
      } else {
        windowInputContainer.classList.add('d-none');
        windowCancelBtn.classList.add('d-none');
      }

      windowOverlay.classList.remove('d-none');
      if (type === 'prompt') windowInput.focus();
    });
  }

  function closeWindow(result) {
    windowOverlay.classList.add('d-none');
    if (windowResolve) {
      if (typeof result === 'undefined' && windowInputContainer.classList.contains('d-none' === false)) {
        windowResolve(windowInput.value);
      } else {
        windowResolve(result);
      }
    }
  }

  windowConfirmBtn?.addEventListener('click', () => {
    const isPrompt = !windowInputContainer.classList.contains('d-none');
    closeWindow(isPrompt ? windowInput.value : true);
  });
  windowCancelBtn?.addEventListener('click', () => closeWindow(null));

  /* ── Focus Guard Logic ── */
  let lastFocusTime = Date.now();
  window.addEventListener('visibilitychange', () => {
    if (document.visibilityState === 'visible') {
      const focusLostOverlay = document.getElementById('focus-lost-overlay');
      if (pomodoroInterval && focusModeSetting) {
        focusLostOverlay?.classList.remove('d-none');
      }
    }
  });

  const chatContent = document.getElementById('chat-content');
  const notesContent = document.getElementById('notes-content');
  const registerButton = document.getElementById('register-button');
  const streakCount = document.getElementById('streak-count');
  const streakMessage = document.getElementById('streak-message');
  const streakActivityButton = document.getElementById('streak-activity-button');
  const featureCards = document.querySelectorAll('.feature-card');
  const focusModeToggle = document.getElementById('focus-mode-toggle');
  const focusModeStatus = document.getElementById('focus-mode-status');
  const distractionOverlay = document.getElementById('distraction-confirm-overlay');
  const distractionContinue = document.getElementById('continue-redirect-btn');
  const distractionStay = document.getElementById('stay-focused-btn');

  const quotes = [
    { text: 'Small steps every day create big progress over time.', author: 'Study Smart' },
    { text: 'The best way to get started is to quit talking and begin doing.', author: 'Walt Disney' },
    { text: 'Focus on being productive instead of busy.', author: 'Tim Ferriss' },
    { text: 'Success is the sum of small efforts repeated day in and day out.', author: 'Robert Collier' },
    { text: 'Don’t watch the clock; do what it does. Keep going.', author: 'Sam Levenson' }
  ];

  const musicTracks = [
    { label: 'Focus Beats', src: 'https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3', category: 'focus', description: 'Energy for deep focus', video: 'https://www.youtube.com/embed/5qap5aO4i9A' },
    { label: 'Calm Study', src: 'https://www.soundhelix.com/examples/mp3/SoundHelix-Song-2.mp3', category: 'focus', description: 'Calm concentration music', video: 'https://www.youtube.com/embed/2Vv-BfVoq4g' },
    { label: 'Motivation Boost', src: 'https://www.soundhelix.com/examples/mp3/SoundHelix-Song-3.mp3', category: 'motivational', description: 'Motivation for study persistence', video: 'https://www.youtube.com/embed/kXYiU_JCYtU' },
    { label: 'Dreamy Romance', src: 'https://www.soundhelix.com/examples/mp3/SoundHelix-Song-4.mp3', category: 'romantic', description: 'Soft romantic vibes for peaceful study', video: 'https://www.youtube.com/embed/JGwWNGJdvx8' },
    { label: 'Night Reflection', src: 'https://www.soundhelix.com/examples/mp3/SoundHelix-Song-5.mp3', category: 'sad', description: 'Mellow sad study melody', video: 'https://www.youtube.com/embed/JkK8g6FMEXE' }
  ];

  const tasksKey = 'studyTasks';
  const flashcardsKey = 'studyFlashcards';
  const streakKey = 'studyStreakCount';
  const streakDateKey = 'studyStreakDate';
  const studyTimeLogKey = 'studyTimeLog';
  const leaderboardDataKey = 'studyLeaderboardData';
  const dailyGoalKey = 'customDailyStudyGoal';
  let dailyGoalMinutes = Number(localStorage.getItem(dailyGoalKey)) || 180;

  function setDailyGoal() {
    showWindow({
      title: 'Set Daily Goal',
      message: 'How many minutes do you want to study every day?',
      type: 'prompt',
      defaultValue: dailyGoalMinutes,
      placeholder: 'e.g. 60, 120, 180'
    }).then(val => {
      const mins = Number(val);
      if (mins && mins >= 30 && mins <= 720) {
        dailyGoalMinutes = mins;
        localStorage.setItem(dailyGoalKey, mins);
        updateStreakPage();
        showWindow({ title: 'Goal Updated', message: `Your new daily goal is ${mins} minutes. Let's crush it!`, type: 'success' });
      } else if (val !== null) {
        showWindow({ title: 'Invalid Goal', message: 'Please enter a value between 30 and 720 minutes.', type: 'warning' });
      }
    });
  }

  const gameUnlockKey = 'studyGameUnlocked';
  const quizQuestions = [
    {
      question: 'What is the best way to stay focused during study?',
      options: ['Multitasking', 'Short focused sessions with breaks', 'Studying for hours without pause', 'Only using your phone'],
      answer: 1
    },
    {
      question: 'Which habit helps build a strong study streak?',
      options: ['Skipping study sometimes', 'Studying at the same time each day', 'Changing routine every hour', 'Studying only before exams'],
      answer: 1
    },
    {
      question: 'When your mind wanders, what should you do?',
      options: ['Keep pushing through', 'Take a short break and return', 'Scroll social media', 'Stop studying forever'],
      answer: 1
    },
    {
      question: 'A healthy focus routine includes:',
      options: ['Continuous work with no rest', 'Balanced study and breaks', 'No planning at all', 'Studying only when bored'],
      answer: 1
    }
  ];
  let currentGameMode = 'memory';
  let currentQuizIndex = 0;
  let quizScore = 0;
  let reactionTimeout = null;
  let reactionStart = null;
  let focusTimer = null;
  let focusRemaining = 0;
  let focusActive = false;

  let pomodoroMode = 'study';
  let studyDurationSeconds = 25 * 60;
  let breakDurationSeconds = 5 * 60;
  let pomodoroSeconds = studyDurationSeconds;
  let pomodoroInterval = null;
  let focusModeSetting = localStorage.getItem('focusModeSetting') === 'true';
  let focusModeActive = false;
  let pendingRedirect = null;
  let tasks = JSON.parse(localStorage.getItem(tasksKey) || '[]');
  let flashcards = JSON.parse(localStorage.getItem(flashcardsKey) || '[]');
  let studySessionStart = null;
  let lastPendingMinuteCount = 0;

  const timerMinutes = document.getElementById('timer-minutes');
  const timerSeconds = document.getElementById('timer-seconds');
  const timerModeLabel = document.getElementById('timer-mode-label');
  const timerCurrentMode = document.getElementById('timer-current-mode');
  const timerProgressRing = document.getElementById('timer-progress-ring');
  const timerSessionMessage = document.getElementById('timer-session-message');
  const timerFullscreenButton = document.getElementById('timer-fullscreen');
  const timerCard = document.querySelector('.timer-card');
  const timerStart = document.getElementById('timer-start');
  const timerPause = document.getElementById('timer-pause');
  const timerReset = document.getElementById('timer-reset');
  const studyDurationInput = document.getElementById('study-duration-input');
  const breakDurationInput = document.getElementById('break-duration-input');
  const studyDurationRange = document.getElementById('study-duration-range');
  const breakDurationRange = document.getElementById('break-duration-range');
  let isTimerFullscreen = false;

  function formatTwoDigits(value) {
    return String(value).padStart(2, '0');
  }

  function updateTimerDisplay() {
    const minutes = Math.floor(pomodoroSeconds / 60);
    const seconds = pomodoroSeconds % 60;
    if (timerMinutes) timerMinutes.textContent = formatTwoDigits(minutes);
    if (timerSeconds) timerSeconds.textContent = formatTwoDigits(seconds);
    updateProgressRing();
  }

  function updateProgressRing() {
    if (!timerProgressRing) return;
    const total = pomodoroMode === 'break' ? breakDurationSeconds : studyDurationSeconds;
    const elapsed = Math.max(0, total - pomodoroSeconds);
    const progressAngle = total > 0 ? Math.min(360, (elapsed / total) * 360) : 0;
    timerProgressRing.style.background = `conic-gradient(#4f46e5 ${progressAngle}deg, #e9ecef ${progressAngle}deg)`;
  }

  function updateTimerSessionMessage() {
    if (!timerSessionMessage) return;
    if (pomodoroInterval) {
      timerSessionMessage.textContent = `Focus session in progress — ${timerModeLabel.textContent}`;
    } else if (pomodoroMode === 'break') {
      timerSessionMessage.textContent = 'Paused: break time ready when you are.';
    } else {
      timerSessionMessage.textContent = 'Ready to start your study session.';
    }
  }

  function updateTimerControls() {
    const isRunning = !!pomodoroInterval;
    if (timerStart) timerStart.disabled = isRunning;
    if (timerPause) timerPause.disabled = !isRunning;
    if (timerReset) timerReset.disabled = false;
    if (timerFullscreenButton) timerFullscreenButton.disabled = false;
    updateTimerSessionMessage();
  }

  function toggleTimerFullscreen() {
    if (!timerCard || !timerFullscreenButton) return;
    if (!document.fullscreenElement) {
      timerCard.requestFullscreen?.().catch(() => { });
    } else {
      document.exitFullscreen?.().catch(() => { });
    }
  }

  document.addEventListener('fullscreenchange', () => {
    isTimerFullscreen = !!document.fullscreenElement;
    if (timerFullscreenButton) {
      timerFullscreenButton.textContent = isTimerFullscreen ? 'Exit Full Screen' : 'Full Screen';
    }
    if (timerCard) timerCard.classList.toggle('fullscreen-card', isTimerFullscreen);
    document.body.classList.toggle('fullscreen-active', isTimerFullscreen);
  });

  function updateTimerSettings() {
    const studyMinutes = Number(studyDurationInput?.value) || 25;
    const breakMinutes = Number(breakDurationInput?.value) || 5;
    studyDurationSeconds = Math.max(60, Math.min(7200, studyMinutes * 60));
    breakDurationSeconds = Math.max(60, Math.min(3600, breakMinutes * 60));
    if (studyDurationInput) studyDurationInput.value = Math.round(studyDurationSeconds / 60);
    if (breakDurationInput) breakDurationInput.value = Math.round(breakDurationSeconds / 60);
    if (studyDurationRange) studyDurationRange.value = Math.round(studyDurationSeconds / 60);
    if (breakDurationRange) breakDurationRange.value = Math.round(breakDurationSeconds / 60);
    if (!pomodoroInterval) {
      pomodoroSeconds = pomodoroMode === 'break' ? breakDurationSeconds : studyDurationSeconds;
      updateTimerDisplay();
    }
  }

  function updateDigitalClock() {
    const clockElement = document.getElementById('digital-clock');
    const dateElement = document.getElementById('clock-date');
    if (!clockElement) return;
    const now = new Date();
    const hours = formatTwoDigits(now.getHours());
    const minutes = formatTwoDigits(now.getMinutes());
    const seconds = formatTwoDigits(now.getSeconds());
    clockElement.textContent = `${hours}:${minutes}:${seconds}`;
    if (dateElement) {
      dateElement.textContent = now.toLocaleDateString(undefined, {
        weekday: 'long',
        year: 'numeric',
        month: 'long',
        day: 'numeric'
      });
    }
  }

  function initDigitalClock() {
    const clockElement = document.getElementById('digital-clock');
    if (!clockElement) return;
    updateDigitalClock();
    setInterval(updateDigitalClock, 1000);
  }

  function updateFocusModeUI() {
    if (focusModeToggle) {
      focusModeToggle.textContent = focusModeSetting ? 'Focus Mode ON' : 'Focus Mode OFF';
      focusModeToggle.className = focusModeSetting ? 'btn btn-sm btn-success' : 'btn btn-sm btn-outline-info';
    }
    if (focusModeStatus) {
      focusModeStatus.textContent = focusModeActive ? 'Focus Mode active' : focusModeSetting ? 'Focus Mode ready' : 'Focus Mode OFF';
      focusModeStatus.className = focusModeActive ? 'badge bg-success' : focusModeSetting ? 'badge bg-warning' : 'badge bg-secondary';
    }
  }

  function setFocusModeSetting(enabled) {
    focusModeSetting = enabled;
    localStorage.setItem('focusModeSetting', String(enabled));
    updateBlockerState();
  }

  function updateBlockerState() {
    focusModeActive = focusModeSetting && !!pomodoroInterval;
    updateFocusModeUI();
  }

  function setPomodoroMode(mode) {
    pomodoroMode = mode;
    pomodoroSeconds = mode === 'break' ? breakDurationSeconds : studyDurationSeconds;
    timerModeLabel.textContent = mode === 'break' ? 'Break' : 'Study';
    timerModeLabel.className = mode === 'break' ? 'badge bg-warning' : 'badge bg-primary';
    if (timerCurrentMode) {
      timerCurrentMode.textContent = mode === 'break' ? 'Break period' : 'Study period';
    }
    updateTimerDisplay();
    updateTimerControls();
    updateBlockerState();
  }

  function startPomodoro() {
    if (pomodoroInterval) return;
    if (pomodoroMode === 'study') {
      studySessionStart = Date.now();
    } else {
      studySessionStart = null;
    }
    pomodoroInterval = setInterval(() => {
      if (pomodoroSeconds > 0) {
        pomodoroSeconds -= 1;
        updateTimerDisplay();
        const pendingMinutes = getPendingStudyMinutes();
        if (pendingMinutes !== lastPendingMinuteCount) {
          lastPendingMinuteCount = pendingMinutes;
          updateStreakPage();
        }
      } else {
        saveStudySessionTime(true);
        clearInterval(pomodoroInterval);
        pomodoroInterval = null;
        lastPendingMinuteCount = 0;
        updateBlockerState();
        updateTimerControls();
        // Play sound alert if enabled
        if (typeof window.timerAlertSound === 'function') {
          try { window.timerAlertSound(); } catch (e) { }
        }
        showWindow({
          title: '🎉 Session Complete!',
          message: 'Amazing work! You finished your session. Time to switch your focus. Your study game is now unlocked.',
          type: 'success'
        });
      }
    }, 1000);
    updateBlockerState();
    updateTimerControls();
  }

  function pausePomodoro() {
    if (pomodoroInterval) {
      saveStudySessionTime();
      clearInterval(pomodoroInterval);
      pomodoroInterval = null;
    }
    updateBlockerState();
    updateTimerControls();
  }

  function resetPomodoro() {
    pausePomodoro();
    pomodoroSeconds = pomodoroMode === 'break' ? breakDurationSeconds : studyDurationSeconds;
    updateTimerDisplay();
    updateBlockerState();
  }

  function showQuote() {
    const quote = quotes[Math.floor(Math.random() * quotes.length)];
    const quoteText = document.getElementById('quote-text');
    const quoteAuthor = document.getElementById('quote-author');
    if (quoteText) quoteText.textContent = quote.text;
    if (quoteAuthor) quoteAuthor.textContent = quote.author;
  }

  function loadTasks() {
    tasks = JSON.parse(localStorage.getItem(tasksKey) || '[]');
    renderTasks();
  }

  function saveTasks() {
    localStorage.setItem(tasksKey, JSON.stringify(tasks));
  }

  function renderTasks() {
    const taskList = document.getElementById('task-list');
    if (!taskList) return;

    const countBadge = document.getElementById('task-count');
    if (countBadge) countBadge.textContent = tasks.filter(t => !t.completed).length;

    if (tasks.length === 0) {
      taskList.innerHTML = '<li class="list-group-item text-center py-4 text-muted fst-italic border-0">No active tasks. Time to relax!</li>';
      return;
    }

    taskList.innerHTML = tasks.map((task, index) => `
        <li class="list-group-item d-flex justify-content-between align-items-center py-3 border-0 rounded-3 mb-2 task-item ${task.completed ? 'completed bg-light' : ''}">
          <div class="d-flex align-items-center gap-3">
            <span class="task-text fw-semibold ${task.completed ? 'text-decoration-line-through text-muted' : 'text-dark'}">${task.title}</span>
          </div>
          <div class="d-flex gap-2">
            <button data-index="${index}" class="btn btn-sm btn-outline-success border-0 task-toggle" title="${task.completed ? 'Undo' : 'Complete'}">
              <i class="fa-solid ${task.completed ? 'fa-rotate-left' : 'fa-check'}"></i>
            </button>
            <button data-index="${index}" class="btn btn-sm btn-outline-danger border-0 task-delete" title="Delete">
              <i class="fa-solid fa-trash-can"></i>
            </button>
          </div>
        </li>
      `).join('');
  }

  function loadFlashcards() {
    flashcards = JSON.parse(localStorage.getItem(flashcardsKey) || '[]');
    renderFlashcards();
  }

  function saveFlashcards() {
    localStorage.setItem(flashcardsKey, JSON.stringify(flashcards));
  }

  function renderFlashcards() {
    const cardsContainer = document.getElementById('flashcards-container');
    if (!cardsContainer) return;
    cardsContainer.innerHTML = flashcards.map((card, index) => `
        <div class="flashcard-item" data-index="${index}">
          <div class="flashcard-side flex-grow-1">
            <div class="fw-semibold">${card.front}</div>
            <div class="text-muted mt-2 flashcard-back flashcard-hidden">${card.back}</div>
          </div>
          <div class="flashcard-actions">
            <button data-index="${index}" class="btn btn-sm btn-outline-primary flashcard-flip">Flip</button>
            <button data-index="${index}" class="btn btn-sm btn-outline-danger flashcard-delete">Delete</button>
          </div>
        </div>
      `).join('');
  }

  function getISODate(date = new Date()) {
    const localDate = new Date(date.getTime() - date.getTimezoneOffset() * 60000);
    return localDate.toISOString().slice(0, 10);
  }

  function getStudyTimeLog() {
    return JSON.parse(localStorage.getItem(studyTimeLogKey) || '{}');
  }

  function saveStudyTimeLog(log) {
    localStorage.setItem(studyTimeLogKey, JSON.stringify(log));
  }

  function getStudyMinutes(date = new Date()) {
    const log = getStudyTimeLog();
    return Number(log[getISODate(date)] || 0);
  }

  function addStudyMinutes(minutes) {
    if (!minutes || minutes < 1) return 0;
    const dateKey = getISODate();
    const log = getStudyTimeLog();
    log[dateKey] = (Number(log[dateKey] || 0) + Math.round(minutes));
    saveStudyTimeLog(log);
    return log[dateKey];
  }

  function getWeeklyStudyMinutes() {
    let total = 0;
    for (let i = 0; i < 7; i += 1) {
      const date = new Date();
      date.setDate(date.getDate() - i);
      total += getStudyMinutes(date);
    }
    return total;
  }

  function getPendingStudyMinutes() {
    if (!studySessionStart || pomodoroMode !== 'study') return 0;
    return Math.max(0, Math.floor((Date.now() - studySessionStart) / 60000));
  }

  function getLevelText(streak) {
    if (streak >= 10) return 'Focus Master';
    if (streak >= 4) return 'Consistent';
    if (streak >= 1) return 'Beginner';
    return 'New Challenger';
  }

  function getMotivationText(streak) {
    if (streak >= 7) return 'You’re ahead of most people 💯';
    if (streak >= 3) return 'You’re building a habit 🔥';
    if (streak === 2) return 'Keep the momentum going!';
    if (streak === 1) return 'Great start!';
    return 'Every day is a new chance to grow.';
  }

  function getSampleLeaderboard() {
    return [
      { name: 'Priya', streak: 8, minutes: 320 },
      { name: 'Arjun', streak: 6, minutes: 280 },
      { name: 'Sneha', streak: 5, minutes: 240 },
      { name: 'Rohan', streak: 4, minutes: 190 },
      { name: 'Ayesha', streak: 2, minutes: 130 }
    ];
  }

  function getLeaderboardData() {
    const stored = JSON.parse(localStorage.getItem(leaderboardDataKey) || 'null');
    return Array.isArray(stored) && stored.length ? stored : getSampleLeaderboard();
  }

  function saveLeaderboardData(entries) {
    localStorage.setItem(leaderboardDataKey, JSON.stringify(entries));
  }

  function buildLeaderboard(userStats) {
    const entries = getLeaderboardData().filter(entry => entry.name !== 'You');
    const combined = [...entries, { name: 'You', streak: userStats.streak, minutes: userStats.minutes, isYou: true }];
    combined.sort((a, b) => {
      if (b.streak !== a.streak) return b.streak - a.streak;
      return b.minutes - a.minutes;
    });
    return combined.map((entry, index) => ({
      rank: index + 1,
      name: entry.name,
      streak: entry.streak,
      minutes: entry.minutes,
      isYou: !!entry.isYou
    }));
  }

  function renderLeaderboard(userStats) {
    const leaderboardList = document.getElementById('leaderboard-list');
    const leaderRank = document.getElementById('leader-rank');
    if (!leaderboardList || !leaderRank) return;

    const entries = buildLeaderboard(userStats);
    leaderboardList.innerHTML = entries.slice(0, 5).map(entry => `
        <li class="leaderboard-list-item ${entry.isYou ? 'leaderboard-you' : ''}">
          <div class="d-flex align-items-center justify-content-between gap-3">
            <div class="d-flex align-items-center gap-3">
              <div class="leaderboard-rank">#${entry.rank}</div>
              <div>
                <div class="fw-semibold">${entry.name}${entry.isYou ? ' (You)' : ''}</div>
                <div class="text-muted">${entry.streak} day streak • ${entry.minutes} min today</div>
              </div>
            </div>
            <span class="badge ${entry.isYou ? 'bg-success' : 'bg-warning text-dark'}">${entry.isYou ? 'Your pace' : 'Top'}</span>
          </div>
        </li>
      `).join('');

    const userRank = entries.find(entry => entry.isYou)?.rank || entries.length;
    leaderRank.textContent = `#${userRank}`;
  }

  function getStreakWarningText(lastDate) {
    const today = getISODate();
    const yesterday = getISODate(new Date(Date.now() - 86400000));
    if (!lastDate) return 'Start today to build your streak.';
    if (lastDate === today) return 'You’re on track today!';
    if (lastDate === yesterday) return 'Don’t break your streak! Come back today 😢';
    return 'A fresh streak begins now. Let’s get back on track.';
  }

  function updateStreakPage() {
    const streakPage = document.getElementById('streak-page');
    if (!streakPage) return;

    const storedCount = Number(localStorage.getItem(streakKey) || 0);
    const lastDate = localStorage.getItem(streakDateKey);
    const todayMinutes = getStudyMinutes();
    const weeklyMinutes = getWeeklyStudyMinutes();
    const pendingMinutes = getPendingStudyMinutes();
    const displayTodayMinutes = todayMinutes + pendingMinutes;
    const displayWeeklyMinutes = weeklyMinutes + pendingMinutes;
    const levelText = getLevelText(storedCount);
    const motivation = getMotivationText(storedCount);
    const warningText = getStreakWarningText(lastDate);
    const goalCompletion = Math.min(100, Math.round((displayTodayMinutes / dailyGoalMinutes) * 100));

    const streakCountElement = document.getElementById('streak-count');
    const streakMessageElement = document.getElementById('streak-message');
    const streakLevel = document.getElementById('streak-level');
    const todayStudyTime = document.getElementById('today-study-time');
    const weeklyStudyTime = document.getElementById('weekly-study-time');
    const goalCompleted = document.getElementById('goal-completed');
    const goalProgressFill = document.getElementById('goal-progress-fill');
    const weeklyProgressFill = document.getElementById('weekly-progress-fill');
    const leaderStreak = document.getElementById('leader-streak');
    const leaderTodayMinutes = document.getElementById('leader-today-minutes');
    const weeklyStudyLog = document.getElementById('weekly-study-log');
    const motivationText = document.getElementById('motivation-text');
    const streakWarning = document.getElementById('streak-warning');
    const goalTargetDisplay = document.getElementById('goal-target-display');

    if (goalTargetDisplay) {
      goalTargetDisplay.textContent = dailyGoalMinutes >= 60 ? `${(dailyGoalMinutes / 60).toFixed(1)} hrs` : `${dailyGoalMinutes} min`;
    }
    if (streakCountElement) streakCountElement.textContent = storedCount;
    if (streakMessageElement) streakMessageElement.textContent = storedCount > 0 ? `${storedCount} Day Streak — ${motivation}` : 'Your streak starts today!';
    if (streakLevel) streakLevel.textContent = levelText;
    if (todayStudyTime) todayStudyTime.textContent = `${displayTodayMinutes} min${pendingMinutes ? ` (+${pendingMinutes} live)` : ''}`;
    if (weeklyStudyTime) weeklyStudyTime.textContent = `${displayWeeklyMinutes} min`;
    if (goalCompleted) goalCompleted.textContent = `${(displayTodayMinutes / 60).toFixed(1)} hrs`;
    if (goalProgressFill) {
      goalProgressFill.style.width = `${goalCompletion}%`;
      goalProgressFill.textContent = `${goalCompletion}%`;
    }
    if (weeklyProgressFill) {
      weeklyProgressFill.style.width = `${Math.min(100, Math.round((displayWeeklyMinutes / (dailyGoalMinutes * 7)) * 100))}%`;
    }
    if (leaderStreak) leaderStreak.textContent = storedCount;
    if (leaderTodayMinutes) leaderTodayMinutes.textContent = `${displayTodayMinutes} min`;
    if (motivationText) motivationText.textContent = motivation;
    if (streakWarning) streakWarning.textContent = warningText;
    renderWeeklyStudyLog(pendingMinutes);
    renderLeaderboard({ streak: storedCount, minutes: displayTodayMinutes });
  }

  function renderWeeklyStudyLog(pendingMinutes = 0) {
    const weeklyStudyLog = document.getElementById('weekly-study-log');
    if (!weeklyStudyLog) return;
    const entries = [];
    const today = new Date();
    for (let i = 6; i >= 0; i -= 1) {
      const date = new Date(today);
      date.setDate(today.getDate() - i);
      const label = i === 0 ? 'Today' : date.toLocaleDateString(undefined, { weekday: 'short' });
      const minutes = getStudyMinutes(date) + (i === 0 ? pendingMinutes : 0);
      entries.push(`
          <li class="weekly-log-item d-flex align-items-center justify-content-between mb-3">
            <div>
              <div class="fw-semibold">${label}</div>
              <div class="text-muted small">${date.toLocaleDateString(undefined, { month: 'short', day: 'numeric' })}</div>
            </div>
            <span class="badge ${minutes >= 60 ? 'bg-warning text-dark' : 'bg-secondary text-white'}">${minutes} min</span>
          </li>
        `);
    }
    weeklyStudyLog.innerHTML = entries.join('');
  }

  function saveStudySessionTime(completed = false) {
    if (!studySessionStart || pomodoroMode !== 'study') {
      studySessionStart = null;
      return;
    }
    const elapsedSeconds = Math.floor((Date.now() - studySessionStart) / 1000);
    const minutes = Math.max(1, Math.round(elapsedSeconds / 60));
    addStudyMinutes(minutes);
    studySessionStart = null;
    updateStreakPage();
    if (completed) {
      localStorage.setItem(gameUnlockKey, 'true');
      if (document.getElementById('game-lock-message')) {
        setupGamePage();
      }
    }
  }

  function isGameUnlocked() {
    return localStorage.getItem(gameUnlockKey) === 'true';
  }

  function switchGameMode(mode) {
    resetReactionGame();
    resetFocusChallenge();
    currentGameMode = mode;
    document.querySelectorAll('.game-mode-btn').forEach(button => {
      button.classList.toggle('active', button.id === `game-mode-${mode}`);
    });
    document.querySelectorAll('.game-content').forEach(section => {
      section.classList.toggle('d-none', section.id !== `game-${mode}`);
    });
    if (mode === 'memory') initGameBoard();
    if (mode === 'quiz') initQuizGame();
    if (mode === 'reaction') resetReactionGame();
    if (mode === 'focus') resetFocusChallenge();
  }

  function renderQuizQuestion() {
    const questionEl = document.getElementById('quiz-question');
    const optionsEl = document.getElementById('quiz-options');
    const scoreEl = document.getElementById('quiz-score');
    if (!questionEl || !optionsEl || !scoreEl) return;

    const current = quizQuestions[currentQuizIndex];
    questionEl.textContent = current.question;
    optionsEl.innerHTML = current.options.map((option, index) => `
        <button type="button" class="btn btn-outline-secondary quiz-option-btn" data-option-index="${index}">${option}</button>
      `).join('');
    scoreEl.textContent = `Score: ${quizScore} / ${quizQuestions.length}`;
  }

  function initQuizGame() {
    currentQuizIndex = 0;
    quizScore = 0;
    renderQuizQuestion();
    const quizStatus = document.getElementById('game-status');
    if (quizStatus) quizStatus.textContent = 'Answer each question to earn a fun completion score.';
  }

  function handleQuizAnswer(selectedIndex) {
    const quizStatus = document.getElementById('game-status');
    if (currentQuizIndex >= quizQuestions.length) {
      if (quizStatus) quizStatus.textContent = `Quiz complete! Your final score is ${quizScore} / ${quizQuestions.length}.`;
      return;
    }
    const question = quizQuestions[currentQuizIndex];
    if (selectedIndex === question.answer) {
      quizScore += 1;
      if (quizStatus) quizStatus.textContent = 'Nice! That one was correct.';
    } else if (quizStatus) {
      quizStatus.textContent = 'Good try — keep going and learn from each question.';
    }
    currentQuizIndex += 1;
    if (currentQuizIndex >= quizQuestions.length) {
      if (quizStatus) quizStatus.textContent = `Quiz complete! Your final score is ${quizScore} / ${quizQuestions.length}.`;
    } else {
      renderQuizQuestion();
    }
  }

  function initReactionGame() {
    const reactionStatus = document.getElementById('reaction-status');
    const reactionResult = document.getElementById('reaction-result');
    const reactionButton = document.getElementById('reaction-start-btn');
    if (!reactionStatus || !reactionResult || !reactionButton) return;
    reactionResult.textContent = '';
    reactionStatus.textContent = 'Get ready... the button will turn green soon.';
    reactionButton.textContent = 'Wait for green';
    if (reactionTimeout) clearTimeout(reactionTimeout);
    reactionStart = null;
    const wait = 1500 + Math.random() * 2500;
    reactionTimeout = setTimeout(() => {
      if (!reactionStatus || !reactionButton) return;
      reactionStatus.textContent = 'Click now!';
      reactionButton.textContent = 'Click!';
      reactionStart = Date.now();
    }, wait);
  }

  function resetReactionGame() {
    const reactionStatus = document.getElementById('reaction-status');
    const reactionResult = document.getElementById('reaction-result');
    const reactionButton = document.getElementById('reaction-start-btn');
    if (reactionTimeout) clearTimeout(reactionTimeout);
    reactionStart = null;
    if (reactionButton) reactionButton.textContent = 'Start Reaction Test';
    if (reactionStatus) reactionStatus.textContent = 'Press start and wait for the green signal.';
    if (reactionResult) reactionResult.textContent = '';
  }

  function initFocusChallenge() {
    const focusStatus = document.getElementById('focus-challenge-status');
    const focusTimerEl = document.getElementById('focus-challenge-timer');
    const durationSelect = document.getElementById('focus-duration');
    if (!focusStatus || !focusTimerEl || !durationSelect) return;
    if (focusTimer) clearInterval(focusTimer);
    focusActive = true;
    focusRemaining = Number(durationSelect.value) || 120;
    const updateTimer = () => {
      const minutes = Math.floor(focusRemaining / 60);
      const seconds = focusRemaining % 60;
      focusTimerEl.textContent = `${formatTwoDigits(minutes)}:${formatTwoDigits(seconds)}`;
      if (focusRemaining <= 0) {
        clearInterval(focusTimer);
        focusActive = false;
        focusStatus.textContent = 'Great work! You completed the focus challenge.';
        return;
      }
      focusRemaining -= 1;
    };
    updateTimer();
    focusStatus.textContent = 'Stay on this page and keep your attention here.';
    focusTimer = setInterval(updateTimer, 1000);
    window.addEventListener('blur', () => {
      if (!focusActive) return;
      clearInterval(focusTimer);
      focusActive = false;
      focusStatus.textContent = 'Focus broken — try again after a full study session.';
    }, { once: true });
  }

  function resetFocusChallenge() {
    const focusStatus = document.getElementById('focus-challenge-status');
    const focusTimerEl = document.getElementById('focus-challenge-timer');
    if (focusTimer) clearInterval(focusTimer);
    focusActive = false;
    focusRemaining = Number(document.getElementById('focus-duration')?.value || 120);
    if (focusTimerEl) focusTimerEl.textContent = `${formatTwoDigits(Math.floor(focusRemaining / 60))}:${formatTwoDigits(focusRemaining % 60)}`;
    if (focusStatus) focusStatus.textContent = 'Press start to begin the Focus Trial.';
  }

  function initGameBoard() {
    const grid = document.getElementById('memory-grid');
    const gameStatus = document.getElementById('game-status');
    if (!grid || !gameStatus) return;

    const icons = ['🧠', '📚', '✨', '🚀'];
    const deck = [...icons, ...icons].sort(() => Math.random() - 0.5);
    let firstCard = null;
    let secondCard = null;
    let lockBoard = false;
    let matches = 0;

    function resetTurn() {
      firstCard = null;
      secondCard = null;
      lockBoard = false;
    }

    function checkForMatch() {
      if (!firstCard || !secondCard) return;
      const match = firstCard.dataset.symbol === secondCard.dataset.symbol;
      if (match) {
        firstCard.classList.add('matched');
        secondCard.classList.add('matched');
        matches += 1;
        gameStatus.textContent = matches === icons.length
          ? 'You matched all pairs! Great focus and well done.'
          : `Nice! ${matches} of ${icons.length} pairs matched.`;
        resetTurn();
      } else {
        lockBoard = true;
        setTimeout(() => {
          [firstCard, secondCard].forEach(card => {
            card.classList.remove('flipped');
            card.querySelector('.card-front')?.classList.remove('d-none');
            card.querySelector('.card-back')?.classList.add('card-hidden');
          });
          resetTurn();
        }, 900);
      }
    }

    grid.innerHTML = deck.map(symbol => `
        <button type="button" class="memory-card" data-symbol="${symbol}">
          <span class="card-front">?</span>
          <span class="card-back card-hidden">${symbol}</span>
        </button>
      `).join('');

    grid.querySelectorAll('.memory-card').forEach(card => {
      card.addEventListener('click', () => {
        if (lockBoard || card.classList.contains('flipped') || card.classList.contains('matched')) return;
        card.classList.add('flipped');
        card.querySelector('.card-front')?.classList.add('d-none');
        card.querySelector('.card-back')?.classList.remove('card-hidden');
        if (!firstCard) {
          firstCard = card;
          return;
        }
        secondCard = card;
        checkForMatch();
      });
    });

    const restartBtn = document.getElementById('restart-game-btn');
    if (restartBtn && !restartBtn.dataset.gameListener) {
      restartBtn.dataset.gameListener = 'true';
      restartBtn.addEventListener('click', initGameBoard);
    }
    gameStatus.textContent = 'Tap two cards to reveal a match.';
  }

  function setupGamePage() {
    const unlocked = isGameUnlocked();
    const gameLocked = document.getElementById('game-locked');
    const gameUnlockedPanel = document.getElementById('game-unlocked');
    const gameUnlockStatus = document.getElementById('game-unlock-status');
    const gameLockMessage = document.getElementById('game-lock-message');
    const gameUnlockHint = document.getElementById('game-unlock-hint');
    if (gameUnlockStatus) {
      gameUnlockStatus.textContent = unlocked ? 'Game unlocked — choose your reward' : 'Unlock the game with focus';
      gameUnlockStatus.className = unlocked ? 'badge bg-success py-2 px-3 fs-6' : 'badge bg-warning py-2 px-3 fs-6';
    }
    if (gameLockMessage) {
      gameLockMessage.textContent = unlocked
        ? 'Study game unlocked! Choose a mini-game and enjoy your reward challenge.'
        : 'Complete a study session to unlock the game.';
    }
    if (gameUnlockHint) {
      gameUnlockHint.textContent = unlocked
        ? 'Great job! The Study Game is unlocked — choose a game below.'
        : 'Complete a study session to unlock the Study Game reward.';
    }
    if (gameLocked) {
      gameLocked.classList.toggle('d-none', unlocked);
    }
    if (gameUnlockedPanel) {
      gameUnlockedPanel.classList.toggle('d-none', !unlocked);
    }
    if (unlocked) {
      switchGameMode(currentGameMode);
    }
  }

  function refreshStreak() {
    const storedCount = Number(localStorage.getItem(streakKey) || 0);
    if (streakCount) streakCount.textContent = storedCount;
    if (streakMessage) streakMessage.textContent = storedCount > 0 ? getMotivationText(storedCount) : 'Your streak starts today!';
    updateStreakPage();
  }

  function recordActivity() {
    const today = getISODate();
    const yesterday = getISODate(new Date(Date.now() - 86400000));
    const lastDate = localStorage.getItem(streakDateKey);
    let count = Number(localStorage.getItem(streakKey) || 0);
    if (lastDate !== today) {
      count = lastDate === yesterday ? count + 1 : 1;
      localStorage.setItem(streakKey, count);
      localStorage.setItem(streakDateKey, today);
    }
    refreshStreak();
  }

  function setupAuthPanels() {
    const registered = isLoggedIn();

    if (chatContent) {
      if (registered) {
        chatContent.innerHTML = `
            <div class="alert alert-success mb-2">Welcome back! Access the student chat below.</div>
            <a href="https://chat.whatsapp.com/Fq87TyqNwL4GAmxRsKUkid?mode=gi_t" target="_blank" rel="noopener noreferrer" data-login-protected="whatsapp" class="btn btn-success">Join the WhatsApp group</a>
            <a class="btn btn-success btn-sm mt-2" target="_blank" rel="noopener noreferrer" href="https://chat.whatsapp.com/Fq87TyqNwL4GAmxRsKUkid?mode=gi_t" data-login-protected="whatsapp">Join Chat</a>
          `;
      } else {
        chatContent.innerHTML = `<div class="alert alert-warning">Please register first. We’ll connect you soon after registration.</div>`;
      }
    }

    if (notesContent) {
      if (registered) {
        notesContent.innerHTML = `
            <div class="row g-3">
              <div class="col-md-4">
                <div class="p-3 bg-light rounded">
                  <h5>Notes</h5>
                  <a target="_blank" rel="noopener noreferrer" href="https://drive.google.com/file/d/1A2B3C4D5E6F7G8H9I/view?usp=sharing" class="d-block link-primary">Class Notes PDF</a>
                </div>
              </div>
              <div class="col-md-4">
                <div class="p-3 bg-light rounded">
                  <h5>PYQs</h5>
                  <a target="_blank" rel="noopener noreferrer" href="https://drive.google.com/file/d/1B2C3D4E5F6G7H8I9J/view?usp=sharing" class="d-block link-primary">Previous Year Questions</a>
                </div>
              </div>
              <div class="col-md-4">
                <div class="p-3 bg-light rounded">
                  <h5>PDFs</h5>
                  <a target="_blank" rel="noopener noreferrer" href="https://drive.google.com/file/d/1C2D3E4F5G6H7I8J9K/view?usp=sharing" class="d-block link-primary">Study Guide PDF</a>
                </div>
              </div>
            </div>
          `;
      } else {
        notesContent.innerHTML = `<div class="alert alert-warning">Please register first to access notes and PDFs.</div>`;
      }
    }

    if (registerButton) {
      if (registered) {
        registerButton.classList.add('d-none');
      } else {
        registerButton.classList.remove('d-none');
        registerButton.addEventListener('click', () => {
          localStorage.setItem('isLoggedIn', 'true');
          localStorage.setItem('studentRegistered', 'true');
          setupAuthPanels();
          showWindow({
            title: 'Registration Success',
            message: 'Your registration is active for this session. Chat and notes are now available.',
            type: 'success'
          });
        }, { once: true });
      }
    }
  }

  function initMusic() {
    const musicCategory = document.getElementById('music-category');
    const musicSelect = document.getElementById('music-select');
    const musicPlayer = document.getElementById('focus-music-player');
    const musicIframe = document.getElementById('music-iframe');
    const categoryDescription = document.getElementById('music-category-description');
    if (!musicSelect || !musicPlayer || !musicCategory || !categoryDescription) return;

    const populateTracks = () => {
      const selectedCategory = musicCategory.value;
      const filtered = musicTracks.filter(track => track.category === selectedCategory);
      musicSelect.innerHTML = '';
      filtered.forEach(track => {
        const option = document.createElement('option');
        option.value = track.src;
        option.dataset.video = track.video || '';
        option.dataset.description = track.description || '';
        option.textContent = track.label;
        musicSelect.appendChild(option);
      });
      if (filtered.length) {
        const firstTrack = filtered[0];
        musicSelect.value = firstTrack.src;
        musicPlayer.src = firstTrack.src;
        musicIframe.src = firstTrack.video || musicIframe.src;
        categoryDescription.textContent = firstTrack.description || 'Pick a mood and play a track to match your study focus.';
      }
    };

    musicCategory.addEventListener('change', populateTracks);
    musicSelect.addEventListener('change', () => {
      const selectedOption = musicSelect.selectedOptions[0];
      if (!selectedOption) return;
      musicPlayer.src = selectedOption.value;
      musicIframe.src = selectedOption.dataset.video || musicIframe.src;
      categoryDescription.textContent = selectedOption.dataset.description || categoryDescription.textContent;
      musicPlayer.play().catch(() => { });
    });

    populateTracks();
  }

  function showDistractionConfirmation() {
    return new Promise(resolve => {
      if (!distractionOverlay || !distractionContinue || !distractionStay) {
        resolve(false);
        return;
      }

      distractionOverlay.classList.remove('d-none');

      const cleanup = () => {
        distractionOverlay.classList.add('d-none');
        distractionContinue.removeEventListener('click', onContinue);
        distractionStay.removeEventListener('click', onStay);
      };

      const onContinue = () => {
        cleanup();
        resolve(true);
      };

      const onStay = () => {
        cleanup();
        resolve(false);
      };

      distractionContinue.addEventListener('click', onContinue);
      distractionStay.addEventListener('click', onStay);
    });
  }

  function playRedirect(link, isBlank) {
    if (isBlank) {
      window.open(link, '_blank', 'noopener');
    } else {
      window.location.href = link;
    }
  }

  function isDistractingLink(url) {
    const distractingSites = ['youtube.com', 'youtu.be', 'instagram.com', 'facebook.com', 'tiktok.com', 'twitter.com', 'x.com', 'reddit.com', 'twitch.tv', 'netflix.com'];
    return distractingSites.some(site => url.hostname.toLowerCase().includes(site));
  }

  function bindInteractions() {
    const timerStart = document.getElementById('timer-start');
    const timerPause = document.getElementById('timer-pause');
    const timerReset = document.getElementById('timer-reset');
    const timerStudyMode = document.getElementById('timer-study-mode');
    const timerBreakMode = document.getElementById('timer-break-mode');
    const reminderSet = document.getElementById('reminder-set');
    const taskAdd = document.getElementById('task-add');
    const taskTitle = document.getElementById('task-title');
    const taskList = document.getElementById('task-list');
    const flashcardAdd = document.getElementById('flashcard-add');
    const flashcardFront = document.getElementById('flashcard-front');
    const flashcardBack = document.getElementById('flashcard-back');
    const cardsContainer = document.getElementById('flashcards-container');

    timerStart?.addEventListener('click', () => {
      startPomodoro();
      recordActivity();
    });
    timerPause?.addEventListener('click', pausePomodoro);
    timerReset?.addEventListener('click', resetPomodoro);
    timerStudyMode?.addEventListener('click', () => setPomodoroMode('study'));
    timerBreakMode?.addEventListener('click', () => setPomodoroMode('break'));
    timerFullscreenButton?.addEventListener('click', toggleTimerFullscreen);

    const syncStudyDuration = () => {
      if (!studyDurationInput || !studyDurationRange) return;
      const value = Math.max(1, Math.min(120, Number(studyDurationInput.value) || 25));
      studyDurationInput.value = value;
      studyDurationRange.value = value;
      updateTimerSettings();
    };

    const syncBreakDuration = () => {
      if (!breakDurationInput || !breakDurationRange) return;
      const value = Math.max(1, Math.min(60, Number(breakDurationInput.value) || 5));
      breakDurationInput.value = value;
      breakDurationRange.value = value;
      updateTimerSettings();
    };

    studyDurationInput?.addEventListener('input', syncStudyDuration);
    studyDurationRange?.addEventListener('input', () => {
      if (studyDurationInput) studyDurationInput.value = studyDurationRange.value;
      updateTimerSettings();
    });
    breakDurationInput?.addEventListener('input', syncBreakDuration);
    breakDurationRange?.addEventListener('input', () => {
      if (breakDurationInput) breakDurationInput.value = breakDurationRange.value;
      updateTimerSettings();
    });

    focusModeToggle?.addEventListener('click', () => setFocusModeSetting(!focusModeSetting));

    reminderSet?.addEventListener('click', async () => {
      const timeInput = document.getElementById('reminder-time');
      if (!timeInput || !timeInput.value) {
        showWindow({ title: 'Pick a Time', message: 'Please choose a time for your study reminder.', type: 'warning' });
        return;
      }
      const [hours, minutes] = timeInput.value.split(':').map(Number);
      const target = new Date();
      target.setHours(hours, minutes, 0, 0);
      if (target <= new Date()) {
        target.setDate(target.getDate() + 1);
      }
      const delay = target.getTime() - Date.now();
      showWindow({
        title: '⏰ Reminder Set',
        message: `Your study reminder is set for ${target.toLocaleTimeString()}. We'll notify you!`,
        type: 'success'
      });
      setTimeout(() => {
        showWindow({ title: '⏰ Study Time!', message: 'It\'s time to start your scheduled study session. Let\'s go!', type: 'alert' });
      }, delay);
    });

    taskAdd?.addEventListener('click', () => {
      if (!taskTitle || !taskTitle.value.trim()) return;
      tasks.push({ title: taskTitle.value.trim(), completed: false });
      taskTitle.value = '';
      saveTasks();
      renderTasks();
      recordActivity();
    });

    taskList?.addEventListener('click', event => {
      const target = event.target;
      const index = target.dataset.index;
      if (typeof index === 'undefined') return;
      const idx = Number(index);
      if (target.classList.contains('task-delete')) {
        tasks.splice(idx, 1);
        saveTasks();
        renderTasks();
      }
      if (target.classList.contains('task-toggle')) {
        tasks[idx].completed = !tasks[idx].completed;
        saveTasks();
        renderTasks();
        recordActivity();
      }
    });

    flashcardAdd?.addEventListener('click', () => {
      if (!flashcardFront || !flashcardBack || !flashcardFront.value.trim() || !flashcardBack.value.trim()) return;
      flashcards.push({ front: flashcardFront.value.trim(), back: flashcardBack.value.trim() });
      flashcardFront.value = '';
      flashcardBack.value = '';
      saveFlashcards();
      renderFlashcards();
      recordActivity();
    });

    document.querySelectorAll('.game-mode-btn').forEach(button => {
      button.addEventListener('click', () => {
        const mode = button.id.replace('game-mode-', '');
        currentGameMode = mode;
        switchGameMode(mode);
      });
    });

    document.getElementById('quiz-options')?.addEventListener('click', event => {
      const button = event.target.closest('button[data-option-index]');
      if (!button) return;
      handleQuizAnswer(Number(button.dataset.optionIndex));
    });

    document.getElementById('quiz-restart-btn')?.addEventListener('click', initQuizGame);
    const reactionBtn = document.getElementById('reaction-start-btn');
    reactionBtn?.addEventListener('click', () => {
      if (reactionStart === null && reactionTimeout) {
        const status = document.getElementById('reaction-status');
        if (status) status.textContent = 'Too early! Wait for the green signal.';
        return;
      }
      if (!reactionStart) {
        initReactionGame();
        return;
      }
      const reactionResult = document.getElementById('reaction-result');
      const reactionStatus = document.getElementById('reaction-status');
      const elapsed = Date.now() - reactionStart;
      if (reactionResult) reactionResult.textContent = `Reaction time: ${elapsed} ms`;
      if (reactionStatus) reactionStatus.textContent = 'Great reflex! Try again to improve.';
      reactionStart = null;
      if (reactionTimeout) {
        clearTimeout(reactionTimeout);
        reactionTimeout = null;
      }
      reactionBtn.textContent = 'Start Reaction Test';
    });
    document.getElementById('focus-start-btn')?.addEventListener('click', initFocusChallenge);
    document.getElementById('focus-duration')?.addEventListener('change', resetFocusChallenge);

    cardsContainer?.addEventListener('click', event => {
      const target = event.target;
      const index = target.dataset.index;
      if (typeof index === 'undefined') return;
      const idx = Number(index);
      if (target.classList.contains('flashcard-delete')) {
        flashcards.splice(idx, 1);
        saveFlashcards();
        renderFlashcards();
      }
      if (target.classList.contains('flashcard-flip')) {
        const cardNode = target.closest('.flashcard-item');
        const backText = cardNode.querySelector('.flashcard-back');
        if (backText) backText.classList.toggle('flashcard-hidden');
        recordActivity();
      }
    });

    streakActivityButton?.addEventListener('click', () => {
      recordActivity();
      showWindow({ title: 'Activity Recorded', message: 'Study activity recorded. Keep your streak going!', type: 'success' });
    });

    const logMinutesButton = document.getElementById('log-minutes-btn');
    logMinutesButton?.addEventListener('click', async () => {
      const input = await showWindow({
        title: 'Log Minutes',
        message: 'Enter minutes of study to log for today:',
        type: 'prompt',
        defaultValue: '30',
        placeholder: 'e.g. 30, 60'
      });
      const minutes = Number(input);
      if (!minutes || minutes <= 0) return;
      addStudyMinutes(minutes);
      updateStreakPage();
      showWindow({ title: 'Minutes Logged', message: `Logged ${minutes} minutes for today. Keep the streak alive!`, type: 'success' });
    });

    featureCards.forEach(card => card.addEventListener('click', recordActivity));

    document.addEventListener('click', async event => {
      const link = event.target.closest('a[href]');
      if (!link) return;
      const href = link.getAttribute('href');
      if (link.dataset.loginProtected && !isLoggedIn()) {
        event.preventDefault();
        await showWindow({
          title: 'Registration Required',
          message: `${loginProtectedMessage} Please complete Register Now first.`,
          type: 'warning'
        });
        triggerRegisterFlow();
        return;
      }

      if (!href || href.startsWith('#') || href.startsWith('mailto:') || href.startsWith('tel:')) {
        return;
      }

      try {
        const targetUrl = new URL(link.href, window.location.href);
        if (targetUrl.origin !== window.location.origin && isDistractingLink(targetUrl) && focusModeActive) {
          event.preventDefault();
          const proceed = await showDistractionConfirmation();
          if (proceed) {
            playRedirect(link.href, link.target === '_blank');
          }
          return;
        }
        if (targetUrl.origin !== window.location.origin && !focusModeActive && isDistractingLink(targetUrl)) {
          // allow navigation to distracting sites when focus mode is disabled
          return;
        }
      } catch (err) {
        // ignore invalid URLs
      }
    });
  }

  document.addEventListener('DOMContentLoaded', () => {
    updateTimerSettings();
    updateTimerDisplay();
    initDigitalClock();
    showQuote();
    loadTasks();
    loadFlashcards();
    refreshStreak();
    setupAuthPanels();
    setupGamePage();
    initMusic();
    updateFocusModeUI();
    bindInteractions();

    const heroRegisterForm = document.getElementById('hero-register-form');
    heroRegisterForm?.addEventListener('submit', () => {
      if (heroRegisterForm.checkValidity()) {
        markUserRegistered();
      }
    });

    const params = new URLSearchParams(window.location.search);
    if (params.get('openRegister') === '1' && !isLoggedIn()) {
      triggerRegisterFlow();
      params.delete('openRegister');
      const cleanQuery = params.toString();
      const cleanUrl = `${window.location.pathname}${cleanQuery ? '?' + cleanQuery : ''}${window.location.hash || ''}`;
      window.history.replaceState({}, '', cleanUrl);
    }
  });
</script>
<div id="full-screen-loader" class="full-screen-loader d-none">
  <div class="loader-spinner"></div>
  <h3 class="fw-bold text-white mb-2">Processing Payment...</h3>
  <p class="text-light opacity-75">Please wait, we are securing your transaction.</p>
</div>
<!-- Motivational Help Bot HTML -->
<% if (typeof hideBot === 'undefined' || !hideBot) { %>
<div class="help-bot-container" id="help-bot">
  <div class="help-bot-window" id="bot-window">
    <div class="bot-header">
      <h5><i class="fa-solid fa-robot me-2 text-primary"></i>2 AM Study Assistant</h5>
      <button type="button" class="btn-close btn-close-white" style="font-size: 0.7rem;" onclick="toggleBot()"></button>
    </div>
    <div class="bot-body" id="bot-chat-body" style="display: flex; flex-direction: column; max-height: 450px;">
      <div id="bot-chat-messages"
        style="flex: 1; overflow-y: auto; padding-right: 5px; margin-bottom: 10px; display: flex; flex-direction: column; gap: 10px;">
        <div class="bot-msg">
          Hey 👋! Main hoon aapka 2AM Study Assistant. Padhai mein koi doubt hai ya phir stress ho raha hai?
        </div>
      </div>

      <div class="bot-options" id="bot-initial-options">
        <button class="bot-opt-btn" onclick="handleBotAction('motivate')">🔥 Motivate Me</button>
        <button class="bot-opt-btn" onclick="handleBotAction('progress')">📊 Check My Progress</button>
        <button class="bot-opt-btn" onclick="handleBotAction('doubt')">🤔 Solve a Doubt</button>
        <button class="bot-opt-btn" onclick="handleBotAction('music')">🎵 I'm feeling bad, play music</button>
      </div>

      <!-- Doubt Solver UI -->
      <div id="bot-doubt-solver" class="d-none mt-2">
        <div class="input-group mb-2">
          <textarea id="bot-doubt-input" class="form-control bg-dark text-white border-secondary" rows="1"
            placeholder="Explain photosynthesis..."
            style="resize: none; font-size: 0.9rem; border-radius: 0.75rem 0 0 0.75rem;"></textarea>
          <button class="btn btn-primary btn-sm" onclick="submitDoubt()" style="border-radius: 0 0.75rem 0.75rem 0;"><i
              class="fa-solid fa-paper-plane"></i></button>
        </div>
        <button class="btn btn-outline-secondary btn-sm w-100 fw-bold" onclick="resetBot()"><i
            class="fa-solid fa-arrow-left me-2"></i>Back to Options</button>
      </div>

      <!-- Progress Mini-Dashboard -->
      <div id="bot-progress-dashboard" class="d-none mt-2">
        <div class="bot-msg" id="bot-progress-content"></div>
        <button class="btn btn-outline-light btn-sm w-100 mt-2" onclick="resetBot()">Back to Options</button>
      </div>
    </div>
  </div>
  <div class="help-bot-bubble" onclick="toggleBot()">
    <div class="pulse"></div>
    <i class="fa-solid fa-comment-dots"></i>
  </div>
</div>
<% } %>

<!-- Firebase Integration for Global Forms -->
<script type="module">
  // Import the functions you need from the SDKs you need
  import { initializeApp } from "https://www.gstatic.com/firebasejs/12.12.0/firebase-app.js";
  import { getFirestore, collection, addDoc, serverTimestamp } from "https://www.gstatic.com/firebasejs/12.12.0/firebase-firestore.js";

  // Your web app's Firebase configuration
  const firebaseConfig = {
    apiKey: "AIzaSyCvJRS3UusvUrVCd2JSicBGL-uRD82GpNM",
    authDomain: "am-study-7ea87.firebaseapp.com",
    projectId: "am-study-7ea87",
    storageBucket: "am-study-7ea87.firebasestorage.app",
    messagingSenderId: "270924691236",
    appId: "1:270924691236:web:7ada12dcf50c290d0925b1"
  };

  let app, db;
  try {
    // Initialize Firebase
    app = initializeApp(firebaseConfig);
    db = getFirestore(app);
  } catch (error) {
    console.error("Error initializing Firebase:", error);
  }

  // Register Service Worker for Browser Push Notifications
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', () => {
      navigator.serviceWorker.register('/firebase-messaging-sw.js').catch(err => {
        console.warn('Service Worker registration notice:', err);
      });
    });
  }

  document.addEventListener("DOMContentLoaded", () => {
    // Intercept generic forms on the site to route them through Firebase
    const forms = document.querySelectorAll("form");

    forms.forEach(form => {
      // Skip forms that have custom submit handlers or specific IDs
      if (
        form.getAttribute('data-custom-submit') === 'true' ||
        form.getAttribute('data-no-firebase') === 'true' ||
        form.classList.contains('no-firebase') ||
        form.classList.contains('sendMailForm') ||
        ['reportFakeProfileForm', 'shopperFeedbackForm', 'loginForm', 'signupForm'].includes(form.id)
      ) {
        return;
      }

      form.addEventListener("submit", async (e) => {
        // Prevent normal backend POST or page reload
        e.preventDefault();

        const submitBtn = form.querySelector('button[type="submit"]') || form.querySelector('.bannerFormBtn');
        const originalBtnText = submitBtn ? submitBtn.innerHTML : 'Submit';

        if (submitBtn) {
          submitBtn.disabled = true;
          submitBtn.innerHTML = '<span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>Processing...';
        }

        // Remove old Firebase alert if it exists
        const oldAlert = form.querySelector('.firebase-alert');
        if (oldAlert) oldAlert.remove();

        // Prepare document data and sanitize File objects
        const formData = new FormData(form);
        const rawData = Object.fromEntries(formData.entries());
        const data = {};
        for (const [key, val] of Object.entries(rawData)) {
          if (val instanceof File) {
            if (val.name) data[key + '_filename'] = val.name;
          } else {
            data[key] = val;
          }
        }
        data.submittedAt = new Date().toISOString();
        data.formName = form.getAttribute('data-form-name') || form.id || 'unidentified_form';

        try {
          if (!db) {
            throw new Error("Firebase is not configured yet. The site owner is currently upgrading this feature!");
          }

          // Use the formName to decide the collection, or default to site_submissions
          let collectionName = "site_submissions";
          if (data.formName.toLowerCase().includes('purchase') || data.formName.toLowerCase().includes('order')) {
            collectionName = "store_orders";
          } else if (data.formName.toLowerCase().includes('contact') || data.formName.toLowerCase().includes('enquiry')) {
            collectionName = "contact_messages";
          }

          // Add to firestore
          data.timestamp = serverTimestamp();
          await addDoc(collection(db, collectionName), data);

          // Success message injection
          const alertDiv = document.createElement('div');
          alertDiv.className = 'alert alert-success mt-3 mb-0 firebase-alert d-flex align-items-center shadow-sm';
          alertDiv.style.borderRadius = '1rem';
          alertDiv.style.fontSize = '0.9rem';
          alertDiv.innerHTML = '<i class="fa-solid fa-circle-check fs-5 me-2 text-success"></i> <div><strong>Success!</strong> Your request has been securely submitted.</div>';
          form.appendChild(alertDiv);

          form.reset();

          // Auto remove success message after 6 seconds
          setTimeout(() => {
            if (alertDiv) alertDiv.remove();
          }, 6000);

        } catch (error) {
          console.error("Firebase write error: ", error);
          // Error message injection
          const alertDiv = document.createElement('div');
          alertDiv.className = 'alert alert-danger mt-3 mb-0 firebase-alert d-flex align-items-center shadow-sm';
          alertDiv.style.borderRadius = '1rem';
          alertDiv.style.fontSize = '0.9rem';
          alertDiv.innerHTML = '<i class="fa-solid fa-triangle-exclamation fs-5 me-2 text-danger"></i> <div><strong>Oops!</strong> ' + error.message + '</div>';
          form.appendChild(alertDiv);
        } finally {
          if (submitBtn) {
            submitBtn.disabled = false;
            submitBtn.innerHTML = originalBtnText;
          }
        }
      });
    });
  });
</script>

<% if (typeof hideBot === 'undefined' || !hideBot) { %>
<script src="/assets/js/bot.js" defer></script>
<% } %>
  <!-- Store Cart Logic -->
  <script>
    async function updateFloatingCart() {
      const container = document.getElementById('storeCartContainer');
      const badge = document.getElementById('floatingCartCount');
      if (!container || !badge) return;
      
      const path = window.location.pathname;
      const isStorePage = path.includes('/store') && 
                          !path.includes('/order-summary') && 
                          !path.includes('/payment') && 
                          !path.includes('/cart');
      
      // Strict path check - Only show on store browse/product pages
      if (isStorePage) {
        container.style.display = 'block';
      } else {
        container.style.display = 'none';
        return;
      }
      
      try {
        const res = await fetch(`/store/api/store/cart`, { credentials: 'include' });
        const data = await res.json();
        if (data.success && data.itemCount > 0) {
          badge.textContent = data.itemCount;
          badge.style.display = 'flex';
        } else {
          badge.style.display = 'none';
        }
      } catch (err) {
        console.error('Failed to sync cart', err);
      }
    }

    document.addEventListener('DOMContentLoaded', updateFloatingCart);
    window.addEventListener('cartUpdated', updateFloatingCart);
    
    // Periodically sync cart state
    setInterval(updateFloatingCart, 10000); 
  </script>
</body>

</html>